<%= form_with(model: pokemon) do |form| %>
  <% if pokemon.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pokemon.errors.count, "error") %> prohibited this pokemon from being saved:</h2>

      <ul>
        <% pokemon.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, "Name" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :element_type, "Type" %>
    <%= form.select :element_type, Pokemon.element_types.keys %>
  </div>

  <div>
    <%= form.label :generation, "Generation" %>
    <%= form.select :generation, Pokemon.generations.keys %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
